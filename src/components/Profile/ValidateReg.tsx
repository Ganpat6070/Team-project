
import React from 'react'

const ValidateReg = (values:any) => {
    let errors = {};

if  (!values.fnamer.trim()){
    // errors.fnamer = "first name should not empty"
}
  return (errors)
}

export default ValidateReg
